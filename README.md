# Vue 3 + Tailwind 4 + Vite Markdown App

Минимально работоспособное веб-приложение на Vue 3 с поддержкой маршрутизации, стилизацией Tailwind CSS v4 и сборкой через Vite.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Локальная разработка
```bash
npm run dev
```
Приложение откроется на `http://localhost:5173`

### Сборка для production
```bash
npm run build
```
Результат в папке `dist/` — готов к развёртыванию на статическом хосте (Nginx, Vercel, Netlify и т.п.).

### Проверка линтера
```bash
npm run lint
```

---

## 📁 Структура проекта

```
.
├── content/                # 📝 Markdown файлы с контентом
│   ├── blog/              # Подпапка "blog"
│   │   ├── post-1.md      # YAML frontmatter + markdown
│   │   └── post-2.md
│   └── docs/              # Подпапка "docs"
│       └── guide.md
├── src/
│   ├── pages/             # Vue компоненты для роутов
│   │   ├── Home.vue       # Главная страница (список постов)
│   │   └── Post.vue       # Страница отдельного поста
│   ├── utils/
│   │   └── load-content.ts  # Загрузка и парсинг Markdown
│   ├── App.vue            # Root компонент
│   ├── main.ts            # Entry point
│   ├── router.ts          # Vue Router конфиг
│   └── style.css          # Глобальные стили + Tailwind
├── docs/                  # Документация проекта
│   ├── project-brief.md   # Спецификация и требования
│   └── adr/               # Architecture Decision Records
├── index.html             # HTML entry point
├── vite.config.ts         # Vite конфиг
├── tsconfig.json          # TypeScript конфиг
└── package.json           # npm зависимости и скрипты
```

---

## 🔑 Ключевые особенности

### ✅ Загрузка Markdown контента

Все Markdown файлы из `content/` автоматически:
- Загружаются при build-time (не на клиенте)
- Запаковываются в JavaScript бандлы
- Парсятся из YAML frontmatter
- Конвертируются в HTML через `marked`

**Пример структуры поста** (`content/blog/my-post.md`):
```markdown
---
title: "Мой первый пост"
date: "2025-12-24"
description: "Короткое описание"
---

# Заголовок

Содержимое поста...
```

### 🗂️ Иерархия директорий сохраняется

Структура `content/` транслируется на клиент:
- `content/blog/post-1.md` → роут `/post/blog/post-1`
- `content/docs/guide.md` → роут `/post/docs/guide`

На главной странице посты группируются по директориям.

### 🎨 Tailwind CSS v4

Все CSS автоматически генерируется из Tailwind классов. Без JIT конфигурации, всё "из коробки".

### 🔀 Vue Router SPA

Переходы по роутам без перезагрузки страницы.

### 📦 Production-ready build

```bash
npm run build
```

В `dist/` лежат:
- `index.html` — готовый HTML
- `assets/` — минифицированные и хешированные JS/CSS бандлы
- Всё готово к развёртыванию на Nginx, Vercel, Netlify

---

## 🛠️ Tech Stack

| Компонент | Технология | Версия |
|-----------|-----------|---------|
| Runtime | Node.js + npm | >=18.0.0 |
| Framework | Vue.js | ^3.4.0 |
| Routing | Vue Router | ^4.3.0 |
| Language | TypeScript | ^5.5.0 |
| Styling | Tailwind CSS | ^4.0.0 |
| Build | Vite | ^5.0.0 |
| Linting | ESLint + Prettier | ^8.0 + ^3.0 |
| Markdown parsing | marked + gray-matter | ^13.0 + ^4.0 |

---

## 📚 Добавление контента

1. Создай поддиректорию в `content/` (например, `content/blog/`)
2. Добавь Markdown файл с YAML frontmatter:

```bash
# Пример
echo '---
title: "Новый пост"
date: "2025-12-25"
---

# Содержимое

Hello World!' > content/blog/new-post.md
```

3. Запусти `npm run dev` — пост автоматически появится на главной странице

---

## 🔗 Маршруты

| Роут | Компонент | Описание |
|------|-----------|----------|
| `/` | `Home.vue` | Главная страница с группированным списком постов |
| `/post/:dir/:id` | `Post.vue` | Отдельный пост (dir = подпапка, id = имя файла без .md) |

---

## 📝 Примечания для разработки

- **Нет state management**: используем Vue Reactive API (ref, computed, provide/inject)
- **Нет тестов**: focus на функциональность
- **TypeScript strict mode**: все типы явные
- **CSS modules не используются**: чистые Tailwind классы

---

## 🚀 Следующие шаги (рекомендации)

1. **Заполнить контент**: добавить примеры постов в `content/`
2. **Кастомизировать стили**: отредактировать цвета и макет в компонентах
3. **SEO оптимизация**: добавить мета-теги (title, description, og:*) при необходимости
4. **Поиск контента**: реализовать поиск по постам (Lunr.js или простой filter)
5. **Деплой**: стягнуть `dist/` на Nginx / Vercel / Netlify

---

## 📄 Лицензия

MIT

