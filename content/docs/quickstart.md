---
title: "Быстрый старт"
date: "2025-12-22"
description: "Как запустить приложение локально"
---

# Быстрый старт

## 1. Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты (Vue, Vite, Tailwind, etc).

## 2. Запуск dev сервера

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173` 🚀

## 3. Изменение кода

- Изменения в `src/` автоматически перезагружаются (HMR)
- Добавление новых постов требует перезагрузки (они загружаются при build-time)

## 4. Сборка production

Когда готовы к развёртыванию:

```bash
npm run build
```

В папке `dist/` появится готовая статика для Nginx / Vercel.

## 5. Проверка качества кода

```bash
npm run lint
```

Эта команда запустит ESLint и исправит ошибки форматирования.

---

Готово! Теперь можно начать разрабатывать своё приложение. 🎉

