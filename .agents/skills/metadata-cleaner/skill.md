# Agent Skill: Metadata Cleaner

## Назначение

Автоматическая очистка метаданных в markdown файлах документации. Удаляет лишние поля метаданных, оставляя только критически важные для поддержки документации.

## Проблема

В процессе разработки документации накапливаются метаданные (Owner, Статус, Для ролей и т.д.), которые:
- Усложняют чтение документов
- Дублируют информацию
- Требуют постоянной синхронизации
- Создают визуальный шум

## Решение

Python-скрипт, который:
1. Сканирует markdown файлы в указанной директории
2. Находит строки метаданных (формат: `- **Поле**: значение`)
3. Удаляет все поля, кроме указанных в whitelist
4. Сохраняет только критичные поля: `Last reviewed` и `Уровень`

## Использование

### Базовое использование

```bash
# Предпросмотр изменений (dry-run)
python .agents/skills/metadata-cleaner/clean_metadata.py --dry-run

# Очистка файлов в текущей директории
python .agents/skills/metadata-cleaner/clean_metadata.py

# Очистка конкретной директории
python .agents/skills/metadata-cleaner/clean_metadata.py -d content/01_Start_Here/
```

### Расширенное использование

```bash
# Рекурсивная обработка всех поддиректорий
python .agents/skills/metadata-cleaner/clean_metadata.py -d content/ -r

# Обработка файлов с другим расширением
python .agents/skills/metadata-cleaner/clean_metadata.py -d docs/ -e .markdown

# Сохранение других полей (переопределение whitelist)
python .agents/skills/metadata-cleaner/clean_metadata.py -k "Owner" -k "Last reviewed"

# Обработка одного файла
python .agents/skills/metadata-cleaner/clean_metadata.py -f content/01_Start_Here/01_01_What_Agents_Are.md
```

## Параметры

| Параметр | Короткий | Описание | По умолчанию |
|----------|----------|----------|--------------|
| `--directory` | `-d` | Директория для обработки | Текущая директория |
| `--file` | `-f` | Обработать один конкретный файл | - |
| `--extension` | `-e` | Расширение файлов | `.md` |
| `--recursive` | `-r` | Рекурсивный обход | `false` |
| `--keep` | `-k` | Поле для сохранения (можно указать несколько) | `Last reviewed`, `Уровень` |
| `--dry-run` | - | Предпросмотр без изменений | `false` |

## Примеры вывода

### Режим предпросмотра (dry-run)

```
🔍 РЕЖИМ ПРЕДПРОСМОТРА
Директория: content/01_Start_Here/
Найдено файлов: 5

🔍 content/01_Start_Here/01_01_What_Agents_Are.md: будет удалено 3 строк(и)
  [-] - **Owner**: DevEx / Platform (TBD)
  [-] - **Статус**: Draft
  [-] - **Для ролей**: Dev / TL / Arch / QA / SRE / Analyst

============================================================
📊 СТАТИСТИКА:
  Обработано файлов: 5
  Изменено файлов: 5
  Удалено строк: 15
  Сохранено строк метаданных: 10
============================================================
```

### Режим выполнения

```
🚀 ОБРАБОТКА ФАЙЛОВ
Директория: content/01_Start_Here/
Найдено файлов: 5

✅ content/01_Start_Here/01_01_What_Agents_Are.md: удалено 3 строк(и)
✅ content/01_Start_Here/01_02_Learning_Paths.md: удалено 3 строк(и)

============================================================
📊 СТАТИСТИКА:
  Обработано файлов: 5
  Изменено файлов: 5
  Удалено строк: 15
  Сохранено строк метаданных: 10
============================================================
```

## Безопасность

- ✅ Всегда используйте `--dry-run` перед первым запуском
- ✅ Скрипт работает только с указанными файлами/директориями
- ✅ Сохраняет кодировку UTF-8
- ✅ Не модифицирует структуру документа, только метаданные
- ⚠️ Создайте backup перед массовой обработкой

## Архитектура

```
MetadataCleaner
├── __init__(keep_fields)          # Инициализация с whitelist полей
├── should_keep_line(line)         # Проверка: сохранить или удалить
├── clean_file(file_path)          # Обработка одного файла
└── clean_directory(directory)     # Обработка директории
```

## Расширение

Скрипт легко расширяется для:
- Добавления новых форматов метаданных
- Трансформации значений полей
- Валидации метаданных
- Генерации отчетов

## Зависимости

- Python 3.7+
- Стандартная библиотека (без внешних зависимостей)

## Автор

AI-DKS Team

## Версия

1.0.0 (2026-01-16)
